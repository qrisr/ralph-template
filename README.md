# Ralph Template

Template für autonome Entwicklung mit Ralph + Claude Code.

## Nutzung

```bash
cp -r ~/ralph-template mein-projekt
cd mein-projekt && git init
./scripts/ralph/ralph.sh      
  10
```
